import { Button, Typography, TextField } from '@mui/material';

export default function Email() {
  return (
    <>
      <Button fullWidth variant="contained" className=" btn-email">
        Sign in with Email
      </Button>
    </>
  );
}
